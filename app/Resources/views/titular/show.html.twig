{% extends 'base.html.twig' %}

{% block body %}
    <h1>Titular</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ titular.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('titular_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('titular_edit', { 'id': titular.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
